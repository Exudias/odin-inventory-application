<!DOCTYPE html>
<head>
    <title><%= title %></title>
</head>
<body>
    <h1>Games: <%= games.length %></h1>
    <div>
        <ul>
        <% for (let i = 0; i < games.length; i++) { %>
            <li><%= games[i].title + " - " + games[i].release_date.toLocaleDateString('en-GB') %></li>
        <% } %>
        </ul>
    </div>
    <a href="/">Home</a>
</body>