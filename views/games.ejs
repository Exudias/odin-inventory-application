<!DOCTYPE html>
<head>
    <title><%= title %></title>
</head>
<body>
    <h1>Games (<%= games.length %>)</h1>
    <div>
        <ul>
        <% for (let i = 0; i < games.length; i++) { %>
            <li><a href="/games/<%= games[i].id %>"><%= games[i].title %></a></li>
        <% } %>
        </ul>
    </div>
    <a href="/">Home</a>
</body>