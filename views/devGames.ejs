<!DOCTYPE html>
<head>
    <title><%= title %></title>
</head>
<body>
    <h1>Developer: <%= devName %></h1>
    <h1>Games:</h1>
    <div>
        <ul>
        <% for (let i = 0; i < games.length; i++) { %>
            <li><a href="/games/<%= games[i].id%>"><%= games[i].title %> (<%= games[i].release_date.getFullYear() %>)</a></li>
        <% } %>
        </ul>
    </div>
</body>